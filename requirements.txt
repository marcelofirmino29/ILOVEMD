# requirements.txt (Adaptado para iLoveMD - Python 3.12)

# --- Core Django ---
Django~=5.2  # Versão principal do Django (use ~= para permitir patches)
asgiref>=3.8,<4  # Dependência comum do Django
sqlparse>=0.5,<0.6 # Dependência comum do Django
tzdata # Necessário para timezones no Django em alguns sistemas

# --- Docling (Biblioteca Principal de Conversão) ---
# Assumindo que instalar 'docling' (via 'pip install docling')
# instala o core e parse necessários. Baseado no README mais recente.
docling

# --- Possíveis dependências ESSENCIAIS do Docling (pip pode instalar automaticamente) ---
# Se após instalar esta lista mínima, o docling der erro ao rodar,
# pode ser necessário adicionar manualmente algumas dessas de volta,
# mas vamos começar sem elas para ver se o pip resolve:
# PyMuPDF>=1.25,<1.26 # Exemplo: para leitura de PDF
# Pillow>=11,<12      # Exemplo: para processamento de imagens em PDFs
# numpy              # Exemplo: comum em processamento

# --- Outras dependências DIRETAS do SEU código (se houver) ---
# Adicione aqui apenas pacotes que VOCÊ importou diretamente
# nos seus arquivos .py (views.py, models.py, etc.),
# que não sejam Django ou docling.
# Exemplo: python-dotenv==1.1.0 (se você usar arquivos .env)